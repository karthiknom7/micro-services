spring:
  application:
    name: limits-service
  cloud:
    config:
      uri:
        - http://localhost:8888
  profiles:
    active: dev
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
